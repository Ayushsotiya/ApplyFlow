export const metadata = {
  title: "All Applications — ApplyFlow",
  description: "Browse and manage your entire job search pipeline in ApplyFlow.",
};

export default function ApplicationsLayout({ children }) {
  return <div className="min-h-screen bg-[#FBFBFD]">{children}</div>;
}
